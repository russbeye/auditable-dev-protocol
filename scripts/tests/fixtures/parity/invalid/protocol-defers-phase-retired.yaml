schema_version: "1.0"

task:
  id: "ADP-015-fixture"
  title: "Parity fixture"
  author: "rbeye"
  date: "2026-08-11"

prompt: |
  A minimal but complete prompt body.

output:
  format: "code"
  destination: "scripts/"

requirements:
  - id: "R1"
    statement: "It does the thing"
    verify: "Run it and look"

protocol:
  apply: true
  defers:
    - phase: "the_loop"
      reason: "Ticketed in the follow-up task instead"
